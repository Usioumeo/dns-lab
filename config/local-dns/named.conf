options {
    directory "/etc/bind";
    recursion yes;
    allow-query { any; };
    query-source port 33333; 
};

zone "example.com" {
    type forward;
    forwarders { 10.9.0.153; };
    forward only; // QUESTA È LA MAGIA CHE IMPEDISCE DI ANDARE SU INTERNET
};
