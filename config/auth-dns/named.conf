options {
    directory "/etc/bind";
    recursion no; // Un server autoritativo non deve fare ricorsione
    allow-query { any; };
};

zone "example.com" {
    type master;
    file "/etc/bind/example.com.zone";
};
