modprobe tun
echo "tun" | tee -a /etc/modules

vi /etc/apk/repositories
apk update
apk add --no-cache python3 docker docker-compose btop bind-tools p7 openssl nano vi

rc-update add docker default
service docker start
addgroup ${USER} docker





git clone https://github.com/Usioumeo/dns-lab.git
chmod +x build-images.sh
./build-images.sh
docker builder prune
docker images -a






cd bind4
docker compose down -t=0  && docker compose up -d && time docker exec -it attacker python3 /attacks/solutions/Cache_poisoning.py
docker compose down -t=0

cd ../bind9
docker compose down -t=0  && docker compose up -d && time docker exec -it attacker python3 /attacks/solutions/Cache_poisoning_rng.py

docker compose down -t=0  && docker compose up -d && time docker exec -it attacker python3 /attacks/solutions/Kaminsky_attack.py
docker compose down -t=0
